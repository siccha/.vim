Installation:
    # git clone git://github.com/nelstrom/dotvim.git ~/.vim
    git clone --recursive git://github.com/ssiccha/.vim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

git clone --recursive should take care of the submodules
# Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update
